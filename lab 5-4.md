# Онлайн худалдааны системийн хураангуй класс диаграм

```mermaid
classDiagram
    class Хэрэглэгч {
        +нэр: String
        +и-мэйл: String
        +хаяг: String
        +бүртгүүлэх()
        +захиалга_үүсгэх()
    }

    class Бүтээгдэхүүн {
        +нэр: String
        +үнэ: Double
        +ангилал: String
        +нөөц: Integer
        +мэдээлэл_харах()
        +нөөцийг_шалгах()
    }

    class Захиалга {
        +хэрэглэгч: String
        +огноо: Date
        +нийт_дүн: Double
        +төлөв: String
        +захиалга_үүсгэх()
        +төлбөр_хийх()
    }

    class Сагс {
        +хэрэглэгч: String
        +бүтээгдэхүүн_нэмэх()
        +захиалга_үүсгэх()
    }

    class Төлбөр {
        +захиалга: String
        +дүн: Double
        +төлбөрийн_арга: String
        +төлбөр_хийх()
    }

    class Хүргэлт {
        +захиалга: String
        +хаяг: String
        +төлөв: String
        +хүргэлт_хийх()
    }

    Хэрэглэгч "1" -- "0..*" Захиалга: include
    Хэрэглэгч "1" -- "0..1" Сагс: include
    Бүтээгдэхүүн "1" -- "0..*" Захиалга: extend
    Захиалга "1" -- "0..1" Төлбөр: include
    Захиалга "1" -- "0..1" Хүргэлт: include
    Сагс -- Бүтээгдэхүүн: include
